/**
 * Placeholder audio feedback hooks for remote navigation. Wire these up to an
 * Audio element or a platform-specific sound API when targeting real hardware.
 */

/**
 * Plays the sound effect for moving focus between navigable items.
 * @returns {void}
 */
export function playFocusSound() {
  // Intentionally a no-op placeholder - hook up a platform sound API here
}

/**
 * Plays the sound effect for confirming a selection (Enter key).
 * @returns {void}
 */
export function playSelectSound() {
  // Intentionally a no-op placeholder - hook up a platform sound API here
}
